// Login page without the need for protection because users will need to access this page to login.
const LoginPage = () => {
    return (
        <div>
            Login Page (UnProtected)
        </div>
    )
}

export default LoginPage