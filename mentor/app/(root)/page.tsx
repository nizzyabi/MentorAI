// Root Page That is protected from users that are not logged in. We used (root) so it can NOT be accessed by typing in the url.

const RootPage = () => {
    return (
        <div>
            Root Page (Protected)
        </div>
    )
}

export default RootPage